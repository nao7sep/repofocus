{
  "name": "repofocus",
  "displayName": "RepoFocus",
  "description": "Shows only actionable Git repositories in VS Code's native Source Control view.",
  "version": "0.1.0",
  "publisher": "nao7sep",
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/nao7sep/repofocus.git"
  },
  "bugs": {
    "url": "https://github.com/nao7sep/repofocus/issues"
  },
  "homepage": "https://github.com/nao7sep/repofocus#readme",
  "engines": {
    "vscode": "^1.131.0"
  },
  "categories": [
    "SCM Providers"
  ],
  "keywords": [
    "git",
    "source-control",
    "multi-repo",
    "repository",
    "vscode-extension"
  ],
  "extensionKind": [
    "workspace"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "extensionDependencies": [
    "vscode.git"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false
    },
    "virtualWorkspaces": {
      "supported": false
    }
  },
  "contributes": {
    "configurationDefaults": {
      "scm.repositories.selectionMode": "multiple"
    },
    "commands": [
      {
        "command": "repofocus.toggle",
        "title": "Toggle Filtering",
        "category": "RepoFocus",
        "icon": "$(filter)"
      },
      {
        "command": "repofocus.refresh",
        "title": "Refresh",
        "category": "RepoFocus",
        "icon": "$(refresh)"
      },
      {
        "command": "repofocus.showAll",
        "title": "Show All Repositories",
        "category": "RepoFocus"
      },
      {
        "command": "repofocus.copyDiagnostics",
        "title": "Copy Diagnostics",
        "category": "RepoFocus"
      },
      {
        "command": "repofocus.revealAll",
        "title": "Reveal All Repositories in Source Control",
        "category": "RepoFocus"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "repofocus.toggle",
          "when": "view == workbench.scm && repofocus.compatible",
          "group": "navigation@100"
        },
        {
          "command": "repofocus.refresh",
          "when": "view == workbench.scm && repofocus.filteringEnabled && repofocus.compatible",
          "group": "navigation@101"
        }
      ]
    },
    "configuration": {
      "title": "RepoFocus",
      "properties": {
        "repofocus.includeUntrackedFiles": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Treat repositories containing untracked files as actionable."
        },
        "repofocus.includeOutgoingCommits": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Treat repositories with outgoing commits or an unpublished branch as actionable."
        },
        "repofocus.includeIncomingCommits": {
          "type": "boolean",
          "default": true,
          "scope": "window",
          "description": "Treat repositories with incoming commits as actionable."
        },
        "repofocus.fetchIntervalMinutes": {
          "type": "integer",
          "default": 5,
          "minimum": 0,
          "maximum": 1440,
          "scope": "window",
          "description": "Minutes between bounded background Git fetches used to detect incoming and outgoing commits. Set to 0 to disable automatic fetching."
        },
        "repofocus.alwaysShow": {
          "type": "array",
          "default": [],
          "scope": "window",
          "maxItems": 100,
          "items": {
            "type": "string",
            "maxLength": 512
          },
          "description": "Git repository glob patterns that remain visible even when clean. A pattern matches a repository's path (workspace-relative where it has one) or its directory name, so a bare name such as 'my-repo' works in any workspace shape."
        },
        "repofocus.minimumRepositoryCount": {
          "type": "integer",
          "default": 2,
          "minimum": 1,
          "maximum": 100,
          "scope": "window",
          "description": "Minimum number of detected Git repositories required before RepoFocus filters clean repositories."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "npm run build && node scripts/run-integration-tests.mjs",
    "check": "npm run typecheck && npm test",
    "vscode:prepublish": "npm run check && npm run build"
  },
  "dependencies": {
    "minimatch": "10.2.6"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/vscode": "1.125.0",
    "@vitest/coverage-v8": "4.1.11",
    "@vscode/test-electron": "3.1.0",
    "@vscode/vsce": "3.9.2",
    "esbuild": "0.28.2",
    "typescript": "^7.0.2",
    "vitest": "4.1.11"
  }
}
